params {
    input       = "data/*"
    outdir      = "results"
    icd_version = "ICD10CM"
    min_count   = 2
}

process {
    container = 'icd-phenotype:latest'
}

podman {
    enabled    = true
    runOptions = '--userns=keep-id'
}